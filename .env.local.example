# --- Copy this file to .env.local ---

# Safe-to-share values (already filled in for your SCAN Scout project).
# The publishable key only allows what Row Level Security permits: reading.
NEXT_PUBLIC_SUPABASE_URL=https://ellofwzolvfrrosedxkh.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_TDbILDflG9616qvYQiADlw_DNIJOu2a

# SECRET — never share or commit. Lets the server (and only the server)
# write matches and results. Find it in the Supabase dashboard:
# Project Settings -> API keys -> "service_role" (or a Secret key).
SUPABASE_SERVICE_ROLE_KEY=paste-your-secret-key-here
